# Tweak when debugging but please make sure that
# this version stays on master. Logging during
# testing is bad, and sending logs anywhere on
# Travis is not useful, so /dev/null it is.
org.slf4j.simpleLogger.logFile=/dev/null
org.slf4j.simpleLogger.defaultLogLevel=error
org.slf4j.simpleLogger.showDateTime=true
